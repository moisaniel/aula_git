Pesquisa pricipais comandos Git
git status

Mostra o estado atual do repositório.
Informa:

Quais arquivos foram modificados

Quais estão prontos para commit (staged)

Quais ainda não foram adicionados

👉 É um dos comandos mais usados para saber “como está o projeto”.

🔹 git add

Adiciona arquivos à área de preparação (staging area).
Isso significa que os arquivos ficarão prontos para serem incluídos no próximo commit.

Exemplos:

git add arquivo.txt → adiciona um arquivo específico

git add . → adiciona todos os arquivos modificados

🔹 git commit

Salva oficialmente as alterações no histórico do repositório.
Cada commit deve ter uma mensagem explicando o que foi feito.

Exemplo:

git commit -m "Correção de erros no login"

🔹 git push

Envia os commits do repositório local para o repositório remoto (ex.: GitHub, GitLab).

Exemplo:

git push origin main

🔹 git checkout

Usado para:

Trocar de branch

Voltar arquivos para uma versão anterior

Exemplos:

git checkout main → muda para a branch main

git checkout -b nova-branch → cria e muda para uma nova branch

🔹 git merge

Combina (mescla) uma branch com outra.
Muito usado para juntar uma branch de desenvolvimento à branch principal.

Exemplo:

git merge feature-login

🔹 git pull

Atualiza o repositório local trazendo as mudanças do repositório remoto.
Ele faz basicamente:

git fetch + git merge

Exemplo:

git pull origin main

🔹 git remote (corrigindo: não é git remotce)

Gerencia os repositórios remotos associados ao projeto.

Exemplos:

git remote → lista os remotos

git remote -v → mostra os endereços dos remotos

git remote add origin URL → adiciona um repositório remotos
